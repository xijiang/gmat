#include <iostream>
#include <vector>

void __read_gt(std::istream&,
	       std::vector<double>&,
	       double&s2pq,
	       std::vector<double>&gt);

void vr1g(std::vector<double>&,
	  double&s,
	  std::vector<double>&,
	  std::ostream&);

void yang(std::vector<double>&,
	  double&,
	  std::vector<double>&,
	  std::ostream&);
