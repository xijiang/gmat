#include <iostream>
#include <vector>

void vr1g(std::vector<double>&,
	  double&s,
	  std::vector<double>&,
	  std::ostream&);

void yang(std::vector<double>&,
	  double&,
	  std::vector<double>&,
	  std::ostream&);
